# Convertido de check_home_dir.sh por convert_shell_to_powershell.ps1\n\n<#\nEste script foi convertido automaticamente de um script shell (.sh) para PowerShell (.ps1).\nAlgumas conversões comuns foram aplicadas, mas é provável que seja necessário ajuste manual.\n\nConversões aplicadas automaticamente:\n- Comentários (# -> #)\n- Variáveis básicas ( -> \)\n\nVerifique e ajuste manualmente:\n- Comandos específicos do Linux (ls, grep, etc.) para equivalentes do PowerShell\n- Redirecionamentos de saída (>, >>, |)\n- Estruturas de controle (if, for, while)\n- Execução de comandos em sequência (;, &&, ||)\n#>\n\n# #!/bin/bash\n\n# Script to check if the home directory is being committed\n# This is a safety measure to prevent accidental recreation of the problematic home directory\n\n# Check if home directory exists and doesn't contain just .gitkeep\nif [ -d "home" ] && [ "$(ls -A home | grep -v '^\.gitkeep$' | wc -l)" -gt 0 ]; then\nWrite-Host "❌ ERROR: The 'home' directory contains files and should not be committed!"\nWrite-Host "   This directory was intentionally removed and should not be recreated."\nWrite-Host "   If you need to store files, please use a different directory."\nWrite-Host "   Files found in 'home' directory:"\n# TODO: Converter comando Linux:   ls -la home/\n  exit 1\nfi\n\nexit 0\n\n\n# Fim do script convertido\n

