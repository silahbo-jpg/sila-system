# Convertido de check_backslashes.sh por convert_shell_to_powershell.ps1\n\n<#\nEste script foi convertido automaticamente de um script shell (.sh) para PowerShell (.ps1).\nAlgumas conversões comuns foram aplicadas, mas é provável que seja necessário ajuste manual.\n\nConversões aplicadas automaticamente:\n- Comentários (# -> #)\n- Variáveis básicas ( -> \)\n\nVerifique e ajuste manualmente:\n- Comandos específicos do Linux (ls, grep, etc.) para equivalentes do PowerShell\n- Redirecionamentos de saída (>, >>, |)\n- Estruturas de controle (if, for, while)\n- Execução de comandos em sequência (;, &&, ||)\n#>\n\n# #!/bin/bash\n\nWrite-Host "🔍 Verificando barras invertidas..."\n\nFILES=$(git diff --cached --name-only --diff-filter=ACM | grep -E '\.(py|sh|ps1|yaml|yml|json|md)$' | while read -r file; do\n    if grep -q '\\' "$file"; then\nWrite-Host "$file"\n    fi\ndone)\n\nif [ -n "$FILES" ]; then\nWrite-Host "❌ Erro: Foram encontradas barras invertidas ('\\') nos seguintes arquivos:"\nWrite-Host "$FILES"\nWrite-Host "\nPor favor, use barras normais (/) para caminhos de arquivo."\n    exit 1\nfi\n\nexit 0\n\n\n# Fim do script convertido\n

